/**
 * Fastn {{ connector.display_name }} connector — auto-generated type declarations.
 *
 * Do not edit manually. Regenerate with `fastn add {{ connector.name }}`.
 */

{% for tool in connector.tools %}
export interface {{ tool.name | pascal_case }}Params {
    {% for param in tool.params %}
    /** {{ param.description }} */
    {{ param.camel_name }}{% if not param.required %}?{% endif %}: {{ param.type | map_type }};
    {% endfor %}
}

{% endfor %}

/**
 * {{ connector.display_name }} connector ({{ connector.category }}).
 *
 * Provides {{ connector.tool_count }} tools.
 */
export declare class {{ connector.class_name }} {
    {% for tool in connector.tools %}
    /**
     * {{ tool.description }}
     */
    {{ tool.camel_name }}(params: {{ tool.name | pascal_case }}Params): Promise<Record<string, any>>;
    {% endfor %}
}
