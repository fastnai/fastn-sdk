"""Fastn placeholder stubs for connectors not yet installed.

Run `fastn connector add <connector>` to get full autocomplete.
"""
from __future__ import annotations

from typing import Any


{% for connector in connectors %}
class {{ connector.class_name }}:
    """{{ connector.display_name }} ({{ connector.category }}).

    Run `fastn connector add {{ connector.name }}` to enable full autocomplete for this connector.
    """

    def __getattr__(self, name: str) -> Any: ...

{% endfor %}
